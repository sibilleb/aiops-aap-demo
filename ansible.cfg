[defaults]
inventory = inventory/localhost.yml
host_key_checking = False
retry_files_enabled = False
# stdout_callback = yaml  # Disabled for compatibility with standard ansible
bin_ansible_callbacks = True

[privilege_escalation]
become = False
